import 'package:flutter/material.dart';

class Myvisits extends StatelessWidget {
  const Myvisits({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
        body: Center(child: Text('My Visits')),
    );
  }
}